import {api} from '../../../lib/api'; export default async function File({params}:{params:Promise<{id:string}>}){const {id}=await params;const f=await api<{path:string;language:string;content:string;indexed_commit_sha:string}>(`/files/${id}`);return <><h2>{f.path}</h2><p className="muted">{f.language} · commit {f.indexed_commit_sha?.slice(0,12)}</p><pre>{f.content.split('\n').map((x,i)=><div id={`L${i+1}`} key={i}><span className="muted">{String(i+1).padStart(4)} </span>{x}</div>)}</pre></>}