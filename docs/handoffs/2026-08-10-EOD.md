# Handoff — knowledge-way, 2026-08-10 (end of day)

Working dir: `/home/artur/Desktop/Projekte/knowledge-way`. Repo: `github.com/ArturL6/knowledge-way`.
Branch state: **`main` frozen at `90b3ba5`** (never merged unattended). **`dev`** holds Batches 1–5.
Current branch: **`batch6/web-foundation`** with WIP commit `3d2b4d7` (Batch 6 #35+#36).

## The working contract (persisted to memory)
See `memory/backlog-execution-contract.md` and `memory/product-north-star.md`. In short: `dev` is the
integration branch off `main`; one feature branch per batch → merged into `dev` with `--no-ff`; the user
does the final `dev`→`main` review. Per issue: **Sonnet implements** (Agent tool, `model: sonnet`) →
**Opus 4.8 reviews in a *fresh* `claude` subagent** (clean context, not a fork) → fix findings → clean
atomic commit → close the issue/epic with a "landed on dev, pending your review" note. If an issue doesn't
hold up, push back **in the issue**. Retrieval/graph changes need **live query validation** (latency + did
the right rows come back), not just unit tests.

## Done this session — 5 batches on `dev` (don't re-derive; read the commit messages)
`git log --oneline main..dev`. Each merge commit + its child commits explain the defect, fix and live
measurement in full:
- **Batch 1 Enforcement** `456c057` — SQLite FK-cascade test harness, web Vitest runner (44 tests), minimal CI (`.github/workflows/ci.yml`). Issues #20/#21/#22, epic #9.
- **Batch 2 Re-index cost** `3aafef4` — migration `0010` indexes the 4 symbol-FK columns. Verified live: Seq Scan cost 9258 → Index Only Scan 25. #23, epic #10. **Live dev DB is at revision 0010**; pre-migration dump at `…/scratchpad/kw-predump-0010.sql`.
- **Batch 3 Graph honesty** `cf20d91` — honest total/returned counts + reason, edge budget (Agent subgraph 2.35MB→85KB), resolution labels replace fake confidence %, distinct-relationship ranking (was 51.7× inflated), 3-way honest empty states. #24–#28, epic #11.
- **Batch 4 Scope** `5b97655` — repository scope pushed into SQL before LIMIT (scoped query no longer starves to 0), deterministic ORDER BY, identity-based fusion dedup. Also added `repository_id` to `/search/symbols` + `/search/semantic`. #29/#30/#31, epic #12.
- **Batch 5 (partial) Perf+correctness** `d5eaef3` — /tree 258→28ms, /callers 1.74s→9ms + honest `total`/`truncated`; 5 correctness fixes (LIKE-escape, chat/files 404s, delete-while-indexing 409, stale progress, search mode/limit validation). #33/#34.

Suite: **78 API + 46 web green** (was 58 at session start). Filed **#54** (hybrid always embeds — latency follow-up).

## In progress — Batch 6 (`batch6/web-foundation`, WIP `3d2b4d7`, NOT merged)
- **#35+#36 implemented**: next/link navigation; /search + /graph URL view-state (restore on reload/Back/Forward); `error.tsx`/`not-found.tsx`/`loading.tsx`; /search 4-state view. Gates passed: vitest 66, tsc, `next build`.
- **Opus 4.8 review of #35+#36: CLEAN** — no blockers, no effect loops/double-fetch (verified against Next 16's reference-stable `useSearchParams`; graph read-side snapshotted into `useRef`), no Batch 3 regression, scope tight. Two **minor** cosmetic items to fold in before merge (non-blocking): (1) `search-client.tsx` empty-state `No results for "{q}"` reads the **live input**, not the searched query — capture the searched query for the message; (2) re-submitting the identical query is a no-op (router.push to same URL doesn't re-run the effect) — nudge if a "retry" is wanted. Full review: `…/tasks/a0d1bc100d25234fa.output`.
- **PENDING before it can merge to `dev`:**
  1. Fold in the 2 minor review items above.
  2. **Real-browser validation** (see Browser section) of Back/Forward/reload preserving search+graph state.
  3. **#37 (a11y), #38 (poll indexing status), #39 (file view)** still unimplemented. File ownership: #37 touches `layout.tsx`+`globals.css`+`graph-canvas.tsx`+forms (overlaps #35/#36 files — do after/careful); #38 = `dashboard-client.tsx`+`lib/repositories.ts` (isolated); #39 = `files/[id]/page.tsx` (isolated). #38/#39 are parallel-safe with each other.

## Browser validation — the thing that unblocks Batch 6 (its gate is interaction/a11y)
The Playwright MCP can't launch here, and the newly-installed **chrome-devtools MCP** hits the same wall: it
looks for Chrome at `/opt/google/chrome/chrome`, which doesn't exist. **Brave IS installed** at
`/opt/brave.com/brave/brave` (v148) and I verified it launches headless with a live CDP endpoint.
`DISPLAY=:1` exists. `~/.cache/ms-playwright/chromium-1223/chrome-linux64/chrome` (Chrome-for-Testing v148)
also works.

Three ways to make the MCP work (pick one, user's call — they leaned toward Brave):
1. **Restart Claude Code** — re-reads my edit to `~/.claude/plugins/cache/claude-plugins-official/chrome-devtools-mcp/1.6.0/.claude-plugin/plugin.json` (I added `-e /opt/brave.com/brave/brave --headless --viewport 1280x800`). No sudo/install. A mid-session `/mcp` reconnect did NOT pick it up (spawn args cached at session start).
2. **sudo symlink** (no restart, uses Brave): `sudo mkdir -p /opt/google/chrome && sudo ln -sf /opt/brave.com/brave/brave /opt/google/chrome/chrome`.
3. **Install google-chrome-stable** (surest; lands exactly at the path the MCP wants).

Once a browser connects: **rebuild the web container from the branch** (`docker compose up -d --build web` — it runs a baked build, not the working tree) then drive it to (a) retroactively validate Batch 3's UI (resolution labels, honest summary, 3 empty states — shipped on unit+tsc only), and (b) validate Batch 6's interactive/a11y gates.

## Decisions/blocks awaiting the user
- **#32** (response models) — HELD with a pushback comment on the issue recommending a *scoped* plan (client-facing routes + OpenAPI snapshot test + documented 404/409/422) over full 42-route modeling, because `response_model` silently drops undeclared fields and could break the web/MCP clients. Epic #13 stays open until this resolves.
- **#52** (POST /sync destroys the vector index) — traced: `ingestion.py:225` deletes every CodeChunk for every file and only re-embeds when `full=True`, so `sync` wipes vectors. Clean fix is entangled with the **stable-symbol-identity decision (#42)** — don't settle unattended.
- **Batch 7 decisions #40–#44** and **Batch 8 workspace (#45–#48)** — need the user; Batch 8 is blocked on the decisions.
- **pydanticAI ~11-min full re-index wall-clock** — the one #23 acceptance measure deliberately deferred (heavy run; user asked to keep indexing to small repos).

## Environment / gotchas (don't relearn these)
- Prior handoff with deeper infra gotchas: `/tmp/knowledge-way-handoff-2026-08-10.md`.
- API tests (working tree, not baked image): `docker compose run --rm --no-deps -w /app -v ./apps/api:/app api python -m pytest tests -q`. Pytest needs the explicit `tests` path or it collects nothing.
- Web container + web MCP validation both run **baked builds** — `docker compose up -d --build <svc>` before validating; my working-tree edits are NOT live until rebuilt.
- `OPENROUTER_API_KEY` lives in `.env` (gitignored) — **never echo/commit it**. `EMBEDDING_PROVIDER=openrouter`, `CODE_CARDS_ENABLED=true`. Spend this session ~$0 (no indexing; only tiny query embeds). Limit $10.
- Two repos indexed: pydanticAI `21ffa409-9e13-493a-b919-7bb6a5b80bb9` (large, 0 embedded), itsdangerous-fork `efb4b844-aeda-4b0c-9bc6-9e8d51dca9cc` (has embeddings) — the small test repo.
- Review artifacts (evidence for every REV-id): `docs/reviews/2026-08-10-comprehensive-review/`.

## Suggested skills
- **chrome-devtools** (`chrome-devtools-mcp:chrome-devtools`) and **`a11y-debugging`** — once a browser is wired, for Batch 6 interaction + #37 accessibility validation.
- **`/review`** — before merging Batch 6 to `dev` (the standing "Opus reviews everything" rule).
- **`/diagnose`** — if a live index/query/browser run misbehaves; both provider failures earlier were only findable by running the thing.
- **`ponytail`** — active at `full`; keep it (it shaped the scoping pushbacks, e.g. #32 and declining a virtualization lib for #39).
- **`/to-issues`** — if new findings accumulate; backlog convention is one issue per PR-sized change, cross-referenced to a REV-id.

## First moves tomorrow
1. Sort the browser (option 1/2/3 above); rebuild web; validate Batch 3 UI + Batch 6 #35/#36 in Brave.
2. Read the #35+#36 review output, apply findings.
3. Implement #37/#38/#39, review each, browser-validate, then merge Batch 6 → `dev`, close #35–#39 + epic #14.
4. Get the user's calls on #32 and #42/#52; then Batches 7–8.
